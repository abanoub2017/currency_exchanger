// Chart.js
import React from 'react';
import { Line } from 'react-chartjs-2';

const Chart = ({ fromTo }) => {
  // Implement the chart based on the selected currencies
  return (
    <div>
      {/* Chart component goes here */}
    </div>
  );
};

export default Chart;
